import type { FastifyPluginAsync } from 'fastify';

const shopifyRoutes: FastifyPluginAsync = async (_fastify) => {
  // ushopify routes - implemented in a later stage
};

export default shopifyRoutes;
