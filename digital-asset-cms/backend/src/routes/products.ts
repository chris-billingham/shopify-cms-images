import type { FastifyPluginAsync } from 'fastify';

const productsRoutes: FastifyPluginAsync = async (_fastify) => {
  // uproducts routes - implemented in a later stage
};

export default productsRoutes;
